# 开发规范

- 手写源代码文件不得超过 800 行；生成文件、依赖、锁文件和数据文件除外。
- 每个文件应职责单一，禁止通过压缩格式或删除可读性内容规避行数限制。
- 单个函数原则上不得超过 60 行，嵌套层级不得超过 3 层。
- 单行代码原则上不得超过 120 个字符。
- 命名必须清晰表达用途，禁止无意义缩写和 `temp`、`data1` 等模糊名称。
- 提交前必须通过格式化、静态检查和现有测试。
- 新增或修改业务行为必须补充测试；修复缺陷必须增加回归测试。
- 禁止提交密钥、令牌、密码及环境专属配置。
- 禁止忽略异常、保留调试代码、无用代码或大段被注释掉的旧代码。
- 新增第三方依赖必须有明确必要性，并同步更新锁文件。
- 注释应解释“为什么这样做”，避免重复描述代码本身。
